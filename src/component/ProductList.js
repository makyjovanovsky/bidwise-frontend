import React from 'react';
import { Paper, Typography, Container, Box } from '@mui/material';
import { spacing } from '@mui/system';

const ProductList = () => {
  return (
    <Container sx={{  bgcolor: '#214434', height: '100vh', borderRadius: '7px'}}>
    </Container>
  );
};

export default ProductList;
